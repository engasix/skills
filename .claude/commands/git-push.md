# Git Push

## Execute the following git commands in sequence

1. `git add .`
2. `git commit -m "$CHANGES"`
3. `git push -u origin main`

**Changes** refers to the current modifications made in the project.
